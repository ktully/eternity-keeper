package uk.me.mantas.eternity.serializer;

public enum SerializerFormat {
	PRESERVE,		// keep input format
	UNITY_2017		// convert down to Steam, GoG  format - if not already in that format
	// TODO: UNITY_2018 if needed
}
